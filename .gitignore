# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets. .env holds the JWT signing key and the AES key that protects every
# encrypted PHI column — it must never reach the repository. .env.example is
# the committed template and carries only placeholders.
.env
.env.*
!.env.example

# Compiled Go binaries
/backend/api
/backend/scheduler
/backend/seed
